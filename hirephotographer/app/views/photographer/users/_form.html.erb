<div class="container full-width">
  <article id="post-2869" class="sixteen columns post-2869 page type-page status-publish hentry">
    <div class="submit-page">
      <%= form_for(photographer_user) do |f| %>
          <% if photographer_user.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(photographer_user.errors.count, 'error') %> prohibited this photographer_user from
                  being saved:</h2>

                <ul>
                  <% photographer_user.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
          <% end %>

          <div class="field form-group  has-feedback <%= 'has-error' if @photographer_user.errors[:username].any? %>">
            <%= f.label :username, :class => 'form-label' %>
            <%= f.text_field :username, :class => 'form-control bg-white form-control-has-validation', :required => true %>
            <%= @photographer_user.errors[:username].join(', ') %>
          </div>

          <div class="form-group field  has-feedback <%= 'has-error' if @photographer_user.errors[:email].any? %>">
            <%= f.label :email, :class => 'form-label' %>
            <%= f.text_field :email, :class => 'form-control bg-white form-control-has-validation', :required => true %>
            <%= @photographer_user.errors[:email].join(', ') %>
          </div>

          <div class="form-group field has-feedback <%= 'has-error' if @photographer_user.errors[:password].any? %>">
            <%= f.label :password, :class => 'form-label' %>
            <%= f.password_field :password, :class => 'form-control bg-white form-control-has-validation', :required => true %>
            <%= @photographer_user.errors[:password].join(', ') %>
          </div>

          <div class="form-group field has-feedback <%= 'has-error' if @photographer_user.errors[:confirmation].any? %>">
            <%= f.label :confirmation, :class => 'form-label' %>
            <%= f.password_field :confirmation, :class => 'form-control bg-white form-control-has-validation', :required => true %>
            <%= @photographer_user.errors[:confirmation].join(', ') %>
          </div>

          <%= f.hidden_field :role, value: 0 %>

          <div class="actions">
            <%= f.submit %>
          </div>
      <% end %>
    </div>
  </article>
</div>